<source>
 @type forward
 port 24224
 bind 0.0.0.0
</source>

<match router.**>
  @type elasticsearch
  host elasticsearch
  port 9200
  index_name fluentd.router
</match>

<match httpd.**>
  @type elasticsearch
  host elasticsearch
  port 9200
  index_name fluentd.container
</match>
